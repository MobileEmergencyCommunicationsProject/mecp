This directory contains an Android NDK import module for NORM, which builds
a shared library, and the JNI bindings.

Building
--------

* Run "ndk-build", which should be in your Android NDK root directory.
* Re-run your ant script to regenerate your APK, which will embed the shared
  libraries.
